import { X } from 'lucide-react';

export const HeaderCloseButton = () => {
  return (
    <div className="flex size-16 items-center justify-center border-x border-gray-700 hover:bg-lineGray">
      <X size={16} />
    </div>
  );
};
